{
  "buildCommand": "pnpm --filter @workspace/cortexa run build",
  "outputDirectory": "artifacts/cortexa/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
